#include <stdio.h>

int main(){

  int x[4]={1,2,3,4};
  int i;

  //for(i = 0; i < 4; i++){

    printf("\nAddresses: %p, %p \n", x, &x[0]);
    printf("\nValues: %d, %d", *x, x[0]);
    
    //}
  

  return 0;
}
